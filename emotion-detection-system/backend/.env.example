# Database — defaults to SQLite (no setup needed, just works)
# Leave this commented out to use SQLite automatically.
# DATABASE_URL=sqlite:///./emotion_detection.db

# Optional: use PostgreSQL instead
# DATABASE_URL=postgresql://user:password@localhost:5432/emotion_detection
